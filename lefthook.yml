commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  commands:
    check:
      run: npm run check
    test:
      run: npx vitest run

pre-push:
  commands:
    typecheck:
      run: npx tsc -b
    test:
      run: npx vitest run
